/*
 * Generated by gdbus-codegen 2.59.0 from org.gnome.Shell.xml. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "flashback-dbus-shell.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.gnome.Shell
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:FlashbackDBusShell
 * @title: FlashbackDBusShell
 * @short_description: Generated C code for the org.gnome.Shell D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.gnome.Shell ---- */

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_eval_IN_ARG_script =
{
  {
    -1,
    (gchar *) "script",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_eval_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_eval_IN_ARG_script,
  NULL
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_eval_OUT_ARG_success =
{
  {
    -1,
    (gchar *) "success",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_eval_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_eval_OUT_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_eval_OUT_ARG_success,
  &_flashback_dbus_shell_method_info_eval_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_eval =
{
  {
    -1,
    (gchar *) "Eval",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_eval_IN_ARG_pointers,
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_eval_OUT_ARG_pointers,
    NULL
  },
  "handle-eval",
  FALSE
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_focus_search =
{
  {
    -1,
    (gchar *) "FocusSearch",
    NULL,
    NULL,
    NULL
  },
  "handle-focus-search",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_show_osd_IN_ARG_params =
{
  {
    -1,
    (gchar *) "params",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_show_osd_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_show_osd_IN_ARG_params,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_show_osd =
{
  {
    -1,
    (gchar *) "ShowOSD",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_show_osd_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-show-osd",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_show_monitor_labels_IN_ARG_params =
{
  {
    -1,
    (gchar *) "params",
    (gchar *) "a{uv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_show_monitor_labels_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_show_monitor_labels_IN_ARG_params,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_show_monitor_labels =
{
  {
    -1,
    (gchar *) "ShowMonitorLabels",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_show_monitor_labels_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-show-monitor-labels",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_show_monitor_labels2_IN_ARG_params =
{
  {
    -1,
    (gchar *) "params",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_show_monitor_labels2_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_show_monitor_labels2_IN_ARG_params,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_show_monitor_labels2 =
{
  {
    -1,
    (gchar *) "ShowMonitorLabels2",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_show_monitor_labels2_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-show-monitor-labels2",
  FALSE
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_hide_monitor_labels =
{
  {
    -1,
    (gchar *) "HideMonitorLabels",
    NULL,
    NULL,
    NULL
  },
  "handle-hide-monitor-labels",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_focus_app_IN_ARG_id =
{
  {
    -1,
    (gchar *) "id",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_focus_app_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_focus_app_IN_ARG_id,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_focus_app =
{
  {
    -1,
    (gchar *) "FocusApp",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_focus_app_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-focus-app",
  FALSE
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_show_applications =
{
  {
    -1,
    (gchar *) "ShowApplications",
    NULL,
    NULL,
    NULL
  },
  "handle-show-applications",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_accelerator =
{
  {
    -1,
    (gchar *) "accelerator",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_flags =
{
  {
    -1,
    (gchar *) "flags",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_accelerator,
  &_flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_flags,
  NULL
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_grab_accelerator_OUT_ARG_action =
{
  {
    -1,
    (gchar *) "action",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_grab_accelerator_OUT_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_grab_accelerator_OUT_ARG_action,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_grab_accelerator =
{
  {
    -1,
    (gchar *) "GrabAccelerator",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_grab_accelerator_IN_ARG_pointers,
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_grab_accelerator_OUT_ARG_pointers,
    NULL
  },
  "handle-grab-accelerator",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_grab_accelerators_IN_ARG_accelerators =
{
  {
    -1,
    (gchar *) "accelerators",
    (gchar *) "a(su)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_grab_accelerators_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_grab_accelerators_IN_ARG_accelerators,
  NULL
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_grab_accelerators_OUT_ARG_actions =
{
  {
    -1,
    (gchar *) "actions",
    (gchar *) "au",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_grab_accelerators_OUT_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_grab_accelerators_OUT_ARG_actions,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_grab_accelerators =
{
  {
    -1,
    (gchar *) "GrabAccelerators",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_grab_accelerators_IN_ARG_pointers,
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_grab_accelerators_OUT_ARG_pointers,
    NULL
  },
  "handle-grab-accelerators",
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_ungrab_accelerator_IN_ARG_action =
{
  {
    -1,
    (gchar *) "action",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_ungrab_accelerator_IN_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_ungrab_accelerator_IN_ARG_action,
  NULL
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_method_info_ungrab_accelerator_OUT_ARG_success =
{
  {
    -1,
    (gchar *) "success",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_method_info_ungrab_accelerator_OUT_ARG_pointers[] =
{
  &_flashback_dbus_shell_method_info_ungrab_accelerator_OUT_ARG_success,
  NULL
};

static const _ExtendedGDBusMethodInfo _flashback_dbus_shell_method_info_ungrab_accelerator =
{
  {
    -1,
    (gchar *) "UngrabAccelerator",
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_ungrab_accelerator_IN_ARG_pointers,
    (GDBusArgInfo **) &_flashback_dbus_shell_method_info_ungrab_accelerator_OUT_ARG_pointers,
    NULL
  },
  "handle-ungrab-accelerator",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _flashback_dbus_shell_method_info_pointers[] =
{
  &_flashback_dbus_shell_method_info_eval,
  &_flashback_dbus_shell_method_info_focus_search,
  &_flashback_dbus_shell_method_info_show_osd,
  &_flashback_dbus_shell_method_info_show_monitor_labels,
  &_flashback_dbus_shell_method_info_show_monitor_labels2,
  &_flashback_dbus_shell_method_info_hide_monitor_labels,
  &_flashback_dbus_shell_method_info_focus_app,
  &_flashback_dbus_shell_method_info_show_applications,
  &_flashback_dbus_shell_method_info_grab_accelerator,
  &_flashback_dbus_shell_method_info_grab_accelerators,
  &_flashback_dbus_shell_method_info_ungrab_accelerator,
  NULL
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_signal_info_accelerator_activated_ARG_action =
{
  {
    -1,
    (gchar *) "action",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _flashback_dbus_shell_signal_info_accelerator_activated_ARG_parameters =
{
  {
    -1,
    (gchar *) "parameters",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _flashback_dbus_shell_signal_info_accelerator_activated_ARG_pointers[] =
{
  &_flashback_dbus_shell_signal_info_accelerator_activated_ARG_action,
  &_flashback_dbus_shell_signal_info_accelerator_activated_ARG_parameters,
  NULL
};

static const _ExtendedGDBusSignalInfo _flashback_dbus_shell_signal_info_accelerator_activated =
{
  {
    -1,
    (gchar *) "AcceleratorActivated",
    (GDBusArgInfo **) &_flashback_dbus_shell_signal_info_accelerator_activated_ARG_pointers,
    NULL
  },
  "accelerator-activated"
};

static const _ExtendedGDBusSignalInfo * const _flashback_dbus_shell_signal_info_pointers[] =
{
  &_flashback_dbus_shell_signal_info_accelerator_activated,
  NULL
};

static const _ExtendedGDBusPropertyInfo _flashback_dbus_shell_property_info_mode =
{
  {
    -1,
    (gchar *) "Mode",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "mode",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _flashback_dbus_shell_property_info_overview_active =
{
  {
    -1,
    (gchar *) "OverviewActive",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "overview-active",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _flashback_dbus_shell_property_info_shell_version =
{
  {
    -1,
    (gchar *) "ShellVersion",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "shell-version",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _flashback_dbus_shell_property_info_pointers[] =
{
  &_flashback_dbus_shell_property_info_mode,
  &_flashback_dbus_shell_property_info_overview_active,
  &_flashback_dbus_shell_property_info_shell_version,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _flashback_dbus_shell_interface_info =
{
  {
    -1,
    (gchar *) "org.gnome.Shell",
    (GDBusMethodInfo **) &_flashback_dbus_shell_method_info_pointers,
    (GDBusSignalInfo **) &_flashback_dbus_shell_signal_info_pointers,
    (GDBusPropertyInfo **) &_flashback_dbus_shell_property_info_pointers,
    NULL
  },
  "dbus-shell",
};


/**
 * flashback_dbus_shell_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
flashback_dbus_shell_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct;
}

/**
 * flashback_dbus_shell_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #FlashbackDBusShell interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
flashback_dbus_shell_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "mode");
  g_object_class_override_property (klass, property_id_begin++, "overview-active");
  g_object_class_override_property (klass, property_id_begin++, "shell-version");
  return property_id_begin - 1;
}



/**
 * FlashbackDBusShell:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link>.
 */

/**
 * FlashbackDBusShellIface:
 * @parent_iface: The parent interface.
 * @handle_eval: Handler for the #FlashbackDBusShell::handle-eval signal.
 * @handle_focus_app: Handler for the #FlashbackDBusShell::handle-focus-app signal.
 * @handle_focus_search: Handler for the #FlashbackDBusShell::handle-focus-search signal.
 * @handle_grab_accelerator: Handler for the #FlashbackDBusShell::handle-grab-accelerator signal.
 * @handle_grab_accelerators: Handler for the #FlashbackDBusShell::handle-grab-accelerators signal.
 * @handle_hide_monitor_labels: Handler for the #FlashbackDBusShell::handle-hide-monitor-labels signal.
 * @handle_show_applications: Handler for the #FlashbackDBusShell::handle-show-applications signal.
 * @handle_show_monitor_labels: Handler for the #FlashbackDBusShell::handle-show-monitor-labels signal.
 * @handle_show_monitor_labels2: Handler for the #FlashbackDBusShell::handle-show-monitor-labels2 signal.
 * @handle_show_osd: Handler for the #FlashbackDBusShell::handle-show-osd signal.
 * @handle_ungrab_accelerator: Handler for the #FlashbackDBusShell::handle-ungrab-accelerator signal.
 * @get_mode: Getter for the #FlashbackDBusShell:mode property.
 * @get_overview_active: Getter for the #FlashbackDBusShell:overview-active property.
 * @get_shell_version: Getter for the #FlashbackDBusShell:shell-version property.
 * @accelerator_activated: Handler for the #FlashbackDBusShell::accelerator-activated signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link>.
 */

typedef FlashbackDBusShellIface FlashbackDBusShellInterface;
G_DEFINE_INTERFACE (FlashbackDBusShell, flashback_dbus_shell, G_TYPE_OBJECT)

static void
flashback_dbus_shell_default_init (FlashbackDBusShellIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * FlashbackDBusShell::handle-eval:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_script: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.Eval">Eval()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_eval() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-eval",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_eval),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * FlashbackDBusShell::handle-focus-search:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.FocusSearch">FocusSearch()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_focus_search() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-focus-search",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_focus_search),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * FlashbackDBusShell::handle-show-osd:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_params: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.ShowOSD">ShowOSD()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_show_osd() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-show-osd",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_show_osd),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * FlashbackDBusShell::handle-show-monitor-labels:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_params: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels">ShowMonitorLabels()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_show_monitor_labels() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-show-monitor-labels",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_show_monitor_labels),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * FlashbackDBusShell::handle-show-monitor-labels2:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_params: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels2">ShowMonitorLabels2()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_show_monitor_labels2() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-show-monitor-labels2",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_show_monitor_labels2),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * FlashbackDBusShell::handle-hide-monitor-labels:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.HideMonitorLabels">HideMonitorLabels()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_hide_monitor_labels() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-hide-monitor-labels",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_hide_monitor_labels),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * FlashbackDBusShell::handle-focus-app:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_id: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.FocusApp">FocusApp()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_focus_app() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-focus-app",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_focus_app),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * FlashbackDBusShell::handle-show-applications:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.ShowApplications">ShowApplications()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_show_applications() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-show-applications",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_show_applications),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * FlashbackDBusShell::handle-grab-accelerator:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_accelerator: Argument passed by remote caller.
   * @arg_flags: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerator">GrabAccelerator()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_grab_accelerator() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-grab-accelerator",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_grab_accelerator),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_UINT);

  /**
   * FlashbackDBusShell::handle-grab-accelerators:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_accelerators: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerators">GrabAccelerators()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_grab_accelerators() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-grab-accelerators",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_grab_accelerators),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * FlashbackDBusShell::handle-ungrab-accelerator:
   * @object: A #FlashbackDBusShell.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_action: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-gnome-Shell.UngrabAccelerator">UngrabAccelerator()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call flashback_dbus_shell_complete_ungrab_accelerator() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-ungrab-accelerator",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, handle_ungrab_accelerator),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT);

  /* GObject signals for received D-Bus signals: */
  /**
   * FlashbackDBusShell::accelerator-activated:
   * @object: A #FlashbackDBusShell.
   * @arg_action: Argument.
   * @arg_parameters: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-gnome-Shell.AcceleratorActivated">"AcceleratorActivated"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("accelerator-activated",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (FlashbackDBusShellIface, accelerator_activated),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_UINT, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * FlashbackDBusShell:mode:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Shell.Mode">"Mode"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("mode", "Mode", "Mode", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * FlashbackDBusShell:overview-active:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Shell.OverviewActive">"OverviewActive"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("overview-active", "OverviewActive", "OverviewActive", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * FlashbackDBusShell:shell-version:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-gnome-Shell.ShellVersion">"ShellVersion"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("shell-version", "ShellVersion", "ShellVersion", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * flashback_dbus_shell_get_mode: (skip)
 * @object: A #FlashbackDBusShell.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Shell.Mode">"Mode"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use flashback_dbus_shell_dup_mode() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
flashback_dbus_shell_get_mode (FlashbackDBusShell *object)
{
  return FLASHBACK_DBUS_SHELL_GET_IFACE (object)->get_mode (object);
}

/**
 * flashback_dbus_shell_dup_mode: (skip)
 * @object: A #FlashbackDBusShell.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-gnome-Shell.Mode">"Mode"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
flashback_dbus_shell_dup_mode (FlashbackDBusShell *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "mode", &value, NULL);
  return value;
}

/**
 * flashback_dbus_shell_set_mode: (skip)
 * @object: A #FlashbackDBusShell.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Shell.Mode">"Mode"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
flashback_dbus_shell_set_mode (FlashbackDBusShell *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "mode", value, NULL);
}

/**
 * flashback_dbus_shell_get_overview_active: (skip)
 * @object: A #FlashbackDBusShell.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Shell.OverviewActive">"OverviewActive"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
flashback_dbus_shell_get_overview_active (FlashbackDBusShell *object)
{
  return FLASHBACK_DBUS_SHELL_GET_IFACE (object)->get_overview_active (object);
}

/**
 * flashback_dbus_shell_set_overview_active: (skip)
 * @object: A #FlashbackDBusShell.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Shell.OverviewActive">"OverviewActive"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
flashback_dbus_shell_set_overview_active (FlashbackDBusShell *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "overview-active", value, NULL);
}

/**
 * flashback_dbus_shell_get_shell_version: (skip)
 * @object: A #FlashbackDBusShell.
 *
 * Gets the value of the <link linkend="gdbus-property-org-gnome-Shell.ShellVersion">"ShellVersion"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use flashback_dbus_shell_dup_shell_version() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
flashback_dbus_shell_get_shell_version (FlashbackDBusShell *object)
{
  return FLASHBACK_DBUS_SHELL_GET_IFACE (object)->get_shell_version (object);
}

/**
 * flashback_dbus_shell_dup_shell_version: (skip)
 * @object: A #FlashbackDBusShell.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-gnome-Shell.ShellVersion">"ShellVersion"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
flashback_dbus_shell_dup_shell_version (FlashbackDBusShell *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "shell-version", &value, NULL);
  return value;
}

/**
 * flashback_dbus_shell_set_shell_version: (skip)
 * @object: A #FlashbackDBusShell.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-gnome-Shell.ShellVersion">"ShellVersion"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
flashback_dbus_shell_set_shell_version (FlashbackDBusShell *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "shell-version", value, NULL);
}

/**
 * flashback_dbus_shell_emit_accelerator_activated:
 * @object: A #FlashbackDBusShell.
 * @arg_action: Argument to pass with the signal.
 * @arg_parameters: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-gnome-Shell.AcceleratorActivated">"AcceleratorActivated"</link> D-Bus signal.
 */
void
flashback_dbus_shell_emit_accelerator_activated (
    FlashbackDBusShell *object,
    guint arg_action,
    GVariant *arg_parameters)
{
  g_signal_emit_by_name (object, "accelerator-activated", arg_action, arg_parameters);
}

/**
 * flashback_dbus_shell_call_eval:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_script: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.Eval">Eval()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_eval_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_eval_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_eval (
    FlashbackDBusShell *proxy,
    const gchar *arg_script,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Eval",
    g_variant_new ("(s)",
                   arg_script),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_eval_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @out_success: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_eval().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_eval().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_eval_finish (
    FlashbackDBusShell *proxy,
    gboolean *out_success,
    gchar **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(bs)",
                 out_success,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_eval_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_script: Argument to pass with the method invocation.
 * @out_success: (out): Return location for return parameter or %NULL to ignore.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.Eval">Eval()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_eval() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_eval_sync (
    FlashbackDBusShell *proxy,
    const gchar *arg_script,
    gboolean *out_success,
    gchar **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Eval",
    g_variant_new ("(s)",
                   arg_script),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(bs)",
                 out_success,
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_focus_search:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.FocusSearch">FocusSearch()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_focus_search_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_focus_search_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_focus_search (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "FocusSearch",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_focus_search_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_focus_search().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_focus_search().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_focus_search_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_focus_search_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.FocusSearch">FocusSearch()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_focus_search() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_focus_search_sync (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "FocusSearch",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_osd:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowOSD">ShowOSD()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_show_osd_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_show_osd_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_show_osd (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ShowOSD",
    g_variant_new ("(@a{sv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_show_osd_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_show_osd().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_show_osd().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_osd_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_osd_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowOSD">ShowOSD()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_show_osd() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_osd_sync (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ShowOSD",
    g_variant_new ("(@a{sv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_monitor_labels:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels">ShowMonitorLabels()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_show_monitor_labels_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_show_monitor_labels_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_show_monitor_labels (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ShowMonitorLabels",
    g_variant_new ("(@a{uv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_show_monitor_labels_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_show_monitor_labels().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_show_monitor_labels().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_monitor_labels_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_monitor_labels_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels">ShowMonitorLabels()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_show_monitor_labels() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_monitor_labels_sync (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ShowMonitorLabels",
    g_variant_new ("(@a{uv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_monitor_labels2:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels2">ShowMonitorLabels2()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_show_monitor_labels2_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_show_monitor_labels2_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_show_monitor_labels2 (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ShowMonitorLabels2",
    g_variant_new ("(@a{sv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_show_monitor_labels2_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_show_monitor_labels2().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_show_monitor_labels2().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_monitor_labels2_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_monitor_labels2_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_params: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels2">ShowMonitorLabels2()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_show_monitor_labels2() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_monitor_labels2_sync (
    FlashbackDBusShell *proxy,
    GVariant *arg_params,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ShowMonitorLabels2",
    g_variant_new ("(@a{sv})",
                   arg_params),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_hide_monitor_labels:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.HideMonitorLabels">HideMonitorLabels()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_hide_monitor_labels_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_hide_monitor_labels_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_hide_monitor_labels (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "HideMonitorLabels",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_hide_monitor_labels_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_hide_monitor_labels().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_hide_monitor_labels().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_hide_monitor_labels_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_hide_monitor_labels_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.HideMonitorLabels">HideMonitorLabels()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_hide_monitor_labels() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_hide_monitor_labels_sync (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "HideMonitorLabels",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_focus_app:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.FocusApp">FocusApp()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_focus_app_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_focus_app_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_focus_app (
    FlashbackDBusShell *proxy,
    const gchar *arg_id,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "FocusApp",
    g_variant_new ("(s)",
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_focus_app_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_focus_app().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_focus_app().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_focus_app_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_focus_app_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_id: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.FocusApp">FocusApp()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_focus_app() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_focus_app_sync (
    FlashbackDBusShell *proxy,
    const gchar *arg_id,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "FocusApp",
    g_variant_new ("(s)",
                   arg_id),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_applications:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowApplications">ShowApplications()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_show_applications_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_show_applications_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_show_applications (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ShowApplications",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_show_applications_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_show_applications().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_show_applications().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_applications_finish (
    FlashbackDBusShell *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_show_applications_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.ShowApplications">ShowApplications()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_show_applications() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_show_applications_sync (
    FlashbackDBusShell *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ShowApplications",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_grab_accelerator:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_accelerator: Argument to pass with the method invocation.
 * @arg_flags: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerator">GrabAccelerator()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_grab_accelerator_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_grab_accelerator_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_grab_accelerator (
    FlashbackDBusShell *proxy,
    const gchar *arg_accelerator,
    guint arg_flags,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GrabAccelerator",
    g_variant_new ("(su)",
                   arg_accelerator,
                   arg_flags),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_grab_accelerator_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @out_action: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_grab_accelerator().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_grab_accelerator().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_grab_accelerator_finish (
    FlashbackDBusShell *proxy,
    guint *out_action,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_action);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_grab_accelerator_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_accelerator: Argument to pass with the method invocation.
 * @arg_flags: Argument to pass with the method invocation.
 * @out_action: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerator">GrabAccelerator()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_grab_accelerator() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_grab_accelerator_sync (
    FlashbackDBusShell *proxy,
    const gchar *arg_accelerator,
    guint arg_flags,
    guint *out_action,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GrabAccelerator",
    g_variant_new ("(su)",
                   arg_accelerator,
                   arg_flags),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(u)",
                 out_action);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_grab_accelerators:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_accelerators: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerators">GrabAccelerators()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_grab_accelerators_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_grab_accelerators_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_grab_accelerators (
    FlashbackDBusShell *proxy,
    GVariant *arg_accelerators,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GrabAccelerators",
    g_variant_new ("(@a(su))",
                   arg_accelerators),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_grab_accelerators_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @out_actions: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_grab_accelerators().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_grab_accelerators().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_grab_accelerators_finish (
    FlashbackDBusShell *proxy,
    GVariant **out_actions,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@au)",
                 out_actions);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_grab_accelerators_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_accelerators: Argument to pass with the method invocation.
 * @out_actions: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerators">GrabAccelerators()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_grab_accelerators() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_grab_accelerators_sync (
    FlashbackDBusShell *proxy,
    GVariant *arg_accelerators,
    GVariant **out_actions,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GrabAccelerators",
    g_variant_new ("(@a(su))",
                   arg_accelerators),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@au)",
                 out_actions);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_ungrab_accelerator:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.UngrabAccelerator">UngrabAccelerator()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_call_ungrab_accelerator_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_call_ungrab_accelerator_sync() for the synchronous, blocking version of this method.
 */
void
flashback_dbus_shell_call_ungrab_accelerator (
    FlashbackDBusShell *proxy,
    guint arg_action,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UngrabAccelerator",
    g_variant_new ("(u)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * flashback_dbus_shell_call_ungrab_accelerator_finish:
 * @proxy: A #FlashbackDBusShellProxy.
 * @out_success: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_call_ungrab_accelerator().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with flashback_dbus_shell_call_ungrab_accelerator().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_ungrab_accelerator_finish (
    FlashbackDBusShell *proxy,
    gboolean *out_success,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_success);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_call_ungrab_accelerator_sync:
 * @proxy: A #FlashbackDBusShellProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @out_success: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-gnome-Shell.UngrabAccelerator">UngrabAccelerator()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_call_ungrab_accelerator() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
flashback_dbus_shell_call_ungrab_accelerator_sync (
    FlashbackDBusShell *proxy,
    guint arg_action,
    gboolean *out_success,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UngrabAccelerator",
    g_variant_new ("(u)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_success);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * flashback_dbus_shell_complete_eval:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @success: Parameter to return.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.Eval">Eval()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_eval (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation,
    gboolean success,
    const gchar *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(bs)",
                   success,
                   result));
}

/**
 * flashback_dbus_shell_complete_focus_search:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.FocusSearch">FocusSearch()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_focus_search (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_show_osd:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.ShowOSD">ShowOSD()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_show_osd (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_show_monitor_labels:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels">ShowMonitorLabels()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_show_monitor_labels (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_show_monitor_labels2:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.ShowMonitorLabels2">ShowMonitorLabels2()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_show_monitor_labels2 (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_hide_monitor_labels:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.HideMonitorLabels">HideMonitorLabels()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_hide_monitor_labels (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_focus_app:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.FocusApp">FocusApp()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_focus_app (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_show_applications:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.ShowApplications">ShowApplications()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_show_applications (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * flashback_dbus_shell_complete_grab_accelerator:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @action: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerator">GrabAccelerator()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_grab_accelerator (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation,
    guint action)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(u)",
                   action));
}

/**
 * flashback_dbus_shell_complete_grab_accelerators:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @actions: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.GrabAccelerators">GrabAccelerators()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_grab_accelerators (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation,
    GVariant *actions)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@au)",
                   actions));
}

/**
 * flashback_dbus_shell_complete_ungrab_accelerator:
 * @object: A #FlashbackDBusShell.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @success: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-gnome-Shell.UngrabAccelerator">UngrabAccelerator()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
flashback_dbus_shell_complete_ungrab_accelerator (
    FlashbackDBusShell *object,
    GDBusMethodInvocation *invocation,
    gboolean success)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   success));
}

/* ------------------------------------------------------------------------ */

/**
 * FlashbackDBusShellProxy:
 *
 * The #FlashbackDBusShellProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * FlashbackDBusShellProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #FlashbackDBusShellProxy.
 */

struct _FlashbackDBusShellProxyPrivate
{
  GData *qdata;
};

static void flashback_dbus_shell_proxy_iface_init (FlashbackDBusShellIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (FlashbackDBusShellProxy, flashback_dbus_shell_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (FlashbackDBusShellProxy)
                         G_IMPLEMENT_INTERFACE (FLASHBACK_TYPE_DBUS_SHELL, flashback_dbus_shell_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (FlashbackDBusShellProxy, flashback_dbus_shell_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (FLASHBACK_TYPE_DBUS_SHELL, flashback_dbus_shell_proxy_iface_init))

#endif
static void
flashback_dbus_shell_proxy_finalize (GObject *object)
{
  FlashbackDBusShellProxy *proxy = FLASHBACK_DBUS_SHELL_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (flashback_dbus_shell_proxy_parent_class)->finalize (object);
}

static void
flashback_dbus_shell_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _flashback_dbus_shell_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
flashback_dbus_shell_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.gnome.Shell: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
flashback_dbus_shell_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _flashback_dbus_shell_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.gnome.Shell", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) flashback_dbus_shell_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
flashback_dbus_shell_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], FLASHBACK_TYPE_DBUS_SHELL);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, FLASHBACK_TYPE_DBUS_SHELL);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
flashback_dbus_shell_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  FlashbackDBusShellProxy *proxy = FLASHBACK_DBUS_SHELL_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
flashback_dbus_shell_proxy_get_mode (FlashbackDBusShell *object)
{
  FlashbackDBusShellProxy *proxy = FLASHBACK_DBUS_SHELL_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Mode");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
flashback_dbus_shell_proxy_get_overview_active (FlashbackDBusShell *object)
{
  FlashbackDBusShellProxy *proxy = FLASHBACK_DBUS_SHELL_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "OverviewActive");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
flashback_dbus_shell_proxy_get_shell_version (FlashbackDBusShell *object)
{
  FlashbackDBusShellProxy *proxy = FLASHBACK_DBUS_SHELL_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ShellVersion");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
flashback_dbus_shell_proxy_init (FlashbackDBusShellProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = flashback_dbus_shell_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, FLASHBACK_TYPE_DBUS_SHELL_PROXY, FlashbackDBusShellProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), flashback_dbus_shell_interface_info ());
}

static void
flashback_dbus_shell_proxy_class_init (FlashbackDBusShellProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = flashback_dbus_shell_proxy_finalize;
  gobject_class->get_property = flashback_dbus_shell_proxy_get_property;
  gobject_class->set_property = flashback_dbus_shell_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = flashback_dbus_shell_proxy_g_signal;
  proxy_class->g_properties_changed = flashback_dbus_shell_proxy_g_properties_changed;

  flashback_dbus_shell_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (FlashbackDBusShellProxyPrivate));
#endif
}

static void
flashback_dbus_shell_proxy_iface_init (FlashbackDBusShellIface *iface)
{
  iface->get_mode = flashback_dbus_shell_proxy_get_mode;
  iface->get_overview_active = flashback_dbus_shell_proxy_get_overview_active;
  iface->get_shell_version = flashback_dbus_shell_proxy_get_shell_version;
}

/**
 * flashback_dbus_shell_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_proxy_new_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
flashback_dbus_shell_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (FLASHBACK_TYPE_DBUS_SHELL_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Shell", NULL);
}

/**
 * flashback_dbus_shell_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with flashback_dbus_shell_proxy_new().
 *
 * Returns: (transfer full) (type FlashbackDBusShellProxy): The constructed proxy object or %NULL if @error is set.
 */
FlashbackDBusShell *
flashback_dbus_shell_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return FLASHBACK_DBUS_SHELL (ret);
  else
    return NULL;
}

/**
 * flashback_dbus_shell_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type FlashbackDBusShellProxy): The constructed proxy object or %NULL if @error is set.
 */
FlashbackDBusShell *
flashback_dbus_shell_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (FLASHBACK_TYPE_DBUS_SHELL_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.gnome.Shell", NULL);
  if (ret != NULL)
    return FLASHBACK_DBUS_SHELL (ret);
  else
    return NULL;
}


/**
 * flashback_dbus_shell_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like flashback_dbus_shell_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call flashback_dbus_shell_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See flashback_dbus_shell_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
flashback_dbus_shell_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (FLASHBACK_TYPE_DBUS_SHELL_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Shell", NULL);
}

/**
 * flashback_dbus_shell_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to flashback_dbus_shell_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with flashback_dbus_shell_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type FlashbackDBusShellProxy): The constructed proxy object or %NULL if @error is set.
 */
FlashbackDBusShell *
flashback_dbus_shell_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return FLASHBACK_DBUS_SHELL (ret);
  else
    return NULL;
}

/**
 * flashback_dbus_shell_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like flashback_dbus_shell_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See flashback_dbus_shell_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type FlashbackDBusShellProxy): The constructed proxy object or %NULL if @error is set.
 */
FlashbackDBusShell *
flashback_dbus_shell_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (FLASHBACK_TYPE_DBUS_SHELL_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.gnome.Shell", NULL);
  if (ret != NULL)
    return FLASHBACK_DBUS_SHELL (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * FlashbackDBusShellSkeleton:
 *
 * The #FlashbackDBusShellSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * FlashbackDBusShellSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #FlashbackDBusShellSkeleton.
 */

struct _FlashbackDBusShellSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_flashback_dbus_shell_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], FLASHBACK_TYPE_DBUS_SHELL);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, FLASHBACK_TYPE_DBUS_SHELL);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_flashback_dbus_shell_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_flashback_dbus_shell_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_flashback_dbus_shell_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _flashback_dbus_shell_skeleton_vtable =
{
  _flashback_dbus_shell_skeleton_handle_method_call,
  _flashback_dbus_shell_skeleton_handle_get_property,
  _flashback_dbus_shell_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
flashback_dbus_shell_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return flashback_dbus_shell_interface_info ();
}

static GDBusInterfaceVTable *
flashback_dbus_shell_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_flashback_dbus_shell_skeleton_vtable;
}

static GVariant *
flashback_dbus_shell_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_flashback_dbus_shell_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _flashback_dbus_shell_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _flashback_dbus_shell_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _flashback_dbus_shell_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.Shell", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _flashback_dbus_shell_emit_changed (gpointer user_data);

static void
flashback_dbus_shell_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _flashback_dbus_shell_emit_changed (skeleton);
}

static void
_flashback_dbus_shell_on_signal_accelerator_activated (
    FlashbackDBusShell *object,
    guint arg_action,
    GVariant *arg_parameters)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u@a{sv})",
                   arg_action,
                   arg_parameters));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.gnome.Shell", "AcceleratorActivated",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void flashback_dbus_shell_skeleton_iface_init (FlashbackDBusShellIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (FlashbackDBusShellSkeleton, flashback_dbus_shell_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (FlashbackDBusShellSkeleton)
                         G_IMPLEMENT_INTERFACE (FLASHBACK_TYPE_DBUS_SHELL, flashback_dbus_shell_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (FlashbackDBusShellSkeleton, flashback_dbus_shell_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (FLASHBACK_TYPE_DBUS_SHELL, flashback_dbus_shell_skeleton_iface_init))

#endif
static void
flashback_dbus_shell_skeleton_finalize (GObject *object)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (flashback_dbus_shell_skeleton_parent_class)->finalize (object);
}

static void
flashback_dbus_shell_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_flashback_dbus_shell_emit_changed (gpointer user_data)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.gnome.Shell",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_flashback_dbus_shell_schedule_emit_changed (FlashbackDBusShellSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
flashback_dbus_shell_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _flashback_dbus_shell_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _flashback_dbus_shell_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
flashback_dbus_shell_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _flashback_dbus_shell_schedule_emit_changed (skeleton, _flashback_dbus_shell_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
flashback_dbus_shell_skeleton_init (FlashbackDBusShellSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = flashback_dbus_shell_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, FLASHBACK_TYPE_DBUS_SHELL_SKELETON, FlashbackDBusShellSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
}

static const gchar *
flashback_dbus_shell_skeleton_get_mode (FlashbackDBusShell *object)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
flashback_dbus_shell_skeleton_get_overview_active (FlashbackDBusShell *object)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
flashback_dbus_shell_skeleton_get_shell_version (FlashbackDBusShell *object)
{
  FlashbackDBusShellSkeleton *skeleton = FLASHBACK_DBUS_SHELL_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
flashback_dbus_shell_skeleton_class_init (FlashbackDBusShellSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = flashback_dbus_shell_skeleton_finalize;
  gobject_class->get_property = flashback_dbus_shell_skeleton_get_property;
  gobject_class->set_property = flashback_dbus_shell_skeleton_set_property;
  gobject_class->notify       = flashback_dbus_shell_skeleton_notify;


  flashback_dbus_shell_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = flashback_dbus_shell_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = flashback_dbus_shell_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = flashback_dbus_shell_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = flashback_dbus_shell_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (FlashbackDBusShellSkeletonPrivate));
#endif
}

static void
flashback_dbus_shell_skeleton_iface_init (FlashbackDBusShellIface *iface)
{
  iface->accelerator_activated = _flashback_dbus_shell_on_signal_accelerator_activated;
  iface->get_mode = flashback_dbus_shell_skeleton_get_mode;
  iface->get_overview_active = flashback_dbus_shell_skeleton_get_overview_active;
  iface->get_shell_version = flashback_dbus_shell_skeleton_get_shell_version;
}

/**
 * flashback_dbus_shell_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-gnome-Shell.top_of_page">org.gnome.Shell</link>.
 *
 * Returns: (transfer full) (type FlashbackDBusShellSkeleton): The skeleton object.
 */
FlashbackDBusShell *
flashback_dbus_shell_skeleton_new (void)
{
  return FLASHBACK_DBUS_SHELL (g_object_new (FLASHBACK_TYPE_DBUS_SHELL_SKELETON, NULL));
}

